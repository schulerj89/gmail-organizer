# Frontend Architecture Sub-Agent

## Mission

Own implementation feasibility for the simplified React dashboard.

## Responsibilities

- Identify low-risk state and component changes.
- Keep backend API contracts stable unless a new workflow requires backend support.
- Preserve existing safety gates for Gmail mutations.
- Use accessible controls and contained scrolling.
- Keep tests/build passing after each slice.

## Current Assignment

Convert `web/src/main.tsx` and `web/src/styles.css` from a lane dashboard into a left-nav cleanup workbench while reusing existing API calls and data types.

